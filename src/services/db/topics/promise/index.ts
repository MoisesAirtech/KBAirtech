import { TopicsType } from "../../../types/Topic";

import { TopicType } from "../../../../types/index"

export const promiseTopic: TopicType = {
    id: 0,
    name: 'promises',
    subTopics: []
}
